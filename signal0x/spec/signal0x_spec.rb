require "spec_helper"

RSpec.describe Signal0x do
  it "has a version number" do
    expect(Signal0x::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
